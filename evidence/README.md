# Validation captures

Selected console output and packet summaries for tenant reachability, isolation, label forwarding and VPNv4 control-plane state. Use the configurations in `configs/` to recreate the topology.
